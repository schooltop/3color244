
<%= simple_form_for @permission, url: save_roles_admin_permissions_path(id:@permission.id) , method: :post, remote: true, html: {id:"add_roles"} do |f| %>
  
    <div id="form_message" class="two fields red" style="margin-left: 15px;display:none;" ></div>

    <% Role.all.each do |role|%>
	    <label class="checkbox">
	      <%= check_box_tag "permission[role_ids][]",role.id,@permission.role_ids.include?(role.id )%> <%= role.name %>
	    </label>
	<% end %>
	<br/>
    
    <div class="field">
      <div class="actions ">
        <div class="ui button primary" id="commition" >提交</div>
        <div class="ui cancel button">取消</div>
      </div>
    </div>

<% end %>


