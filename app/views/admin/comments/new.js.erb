$('#comment_modal').remove();
$('body').append('<div class="ui modal" id="comment_modal"><i class="close icon"></i><h2 class="ui dividing header">添加评论</h2><%= j(render 'new') %></div>');
$('#comment_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();

// TODO表单校验
  $("#commition").click(function(){
    if( $("#comment_content").val() !="" ){
      $("#new_comment").submit(); 
    }else{
      $("#form_message").text("请完善信息！").show(200);
    }
  });

$('#comment_blog_id').change(function(){
  	  var blog_id = $('#comment_blog_id').val()
  	  if (blog_id != ""){
	  	  $.ajax({
	          url: "/admin/comments/show_name",
	          type: "get",
	          data: {id: blog_id},
	          success: function(data){
	            $("#show_name").html(data.name )
	          }
	        });
	      }
    })
