$('#vendor_modal').remove();
$('body').append('<div class="ui modal" id="vendor_modal"><i class="close icon"></i><h2 class="ui dividing header">添加合作企业</h2><%= j(render 'new') %></div>');
$('#vendor_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();

// TODO表单校验
  $("#commition").click(function(){
    if( $("#vendor_name").val() !="" ){
      $("#vendor_form").submit(); 
    }else{
      $("#form_message").text("请完善信息！").show(200);
    }
  });


  //富文本编辑器初始化
  var editor = new wangEditor('vendor_content');
  //图片上传配置
  editor.config.uploadImgUrl = "/admin/vendors/upload_image";
  editor.config.uploadImgFileName = "wang_editor_file"
  editor.config.uploadHeaders = {
  'Accept' : 'text/x-json',
  'X-CSRF-Token': $('meta[name="csrf-token"]').attr('content')
  };
  editor.create();
  editor.$editorContainer.css('z-index', 0);