<div class="ui grid">
  <div class="left floated fourteen wide column">
    <h2><a href="javascript:history.back()"><i class="blue reply icon"></i></a>合作企业管理</h2>
  </div>
</div>
<%= render 'search_form'  %>

<div class="ui blue segment">

    <div class="ui grid">
      <div class="left floated wide column">
        <%= link_to '新增合作商', new_admin_vendor_path, class: 'ui primary button', remote: true  %>
      </div>
    </div>
  <table class="ui selectable striped celled table">
    <thead>
    <tr >
      <th class="center aligned">ID</th>
      <th class="center aligned">邮箱</th>
      <th class="center aligned">名称</th>
      <th class="center aligned">操作</th>
    </tr>
    </thead>
    <tbody>
    <%= render partial: 'list', collection: @vendors, as: :vendor %>
    </tbody>
  </table>
  <span class="blue">
    <%= page_entries_info(@vendors) %>
  </span>
  <%= paginate(@vendors, :theme => 'admin')%>
</div>