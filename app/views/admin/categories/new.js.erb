$('#category_modal').remove();
$('body').append('<div class="ui modal" id="category_modal"><i class="close icon"></i><h2 class="ui dividing header">添加类型</h2><%= j(render 'new') %></div>');
$('#category_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();

// TODO表单校验
  $("#commition").click(function(){
    if( $("#category_name").val() !="" ){
      $("#new_category").submit(); 
    }else{
      $("#form_message").text("请完善信息！").show(200);
    }
  });

  $('#category_parent_id').change(function(){
  	  var parent_id = $('#category_parent_id').val()
  	  if (parent_id != ""){
	  	  $.ajax({
	          url: "/admin/categories/show_name",
	          type: "get",
	          data: {id: parent_id},
	          success: function(data){
	            $("#show_name").html(data.name)
	          }
	        });
	      }
    })
