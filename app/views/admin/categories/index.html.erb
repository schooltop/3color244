<div class="ui grid">
  <div class="left floated fourteen wide column">
    <h2><a href="javascript:history.back()"><i class="blue reply icon"></i></a>分类管理</h2>
  </div>
</div>

 <%= render :partial => "search_form" %>

  <div class="ui blue segment">
    <div class="ui grid">
      <div class="left floated wide column">
        <%= link_to '新增分类', new_admin_category_path, class: 'ui primary button', remote: true  %>
      </div>
    </div>
    <table class="ui selectable striped celled table">
      <thead>
        <tr>
          <th class="center aligned">名称</th>
          <th class="center aligned">模块</th>
          <th class="center aligned">父ID</th>
          <th class="center aligned">操作</th>
        </tr>
      </thead>
      <tbody>
        <%= render :partial => "list", :collection => @categories, :as => :category %>
      </tbody>
    </table>
    <span class="blue">
      <%= page_entries_info(@categories) %>
    </span>
    <%= paginate(@categories, :theme => 'admin')%>
  </div>